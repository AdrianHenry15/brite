import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "Contact Us At Brite",
    description: "Contact Us",
};

export default function ContactUsPage() {
    return <section></section>;
}
